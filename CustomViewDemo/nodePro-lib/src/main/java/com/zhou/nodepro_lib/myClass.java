package com.zhou.nodepro_lib;

public class myClass {

    public myClass() {
        /*CommandReceiver receiver = new CommandReceiver();
        byte [] data = {12,3,4,5,6};
        try {
            receiver.parsingCommand(data, new CommandReceiver.NodeType() {
                @Override
                public void getParsingType(CommandType type, Object o) {
                    switch (type){
                        case PASS_CAR:
                            PassCarInfo info = (PassCarInfo) o;
                            break;
                    }
                }
            });

            receiver.parsingCommand(data,(type,o) ->{
                switch (type){
                    case PASS_CAR:
                        PassCarInfo info = (PassCarInfo) o;
                        break;
                }
            });

        } catch (Exception e) {
            System.out.print(e.toString());
        }
*/
    }
}
